.. _updating:

Updating
========

If using the :ref:`installation-docker`, any actions required by a backend upgrade will be handled by
just updating the image tag and recreating the container.

Before updating, make sure you check the :ref:`changelog` for any notes about the changes. Sometimes extra manual steps might be required or an update could take a long time due to database migrations.
